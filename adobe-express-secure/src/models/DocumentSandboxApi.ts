// This interface declares all the APIs that the document sandbox runtime ( i.e. code.ts ) exposes to the UI/iframe runtime
export interface DocumentSandboxApi {
  // createRectangle(): void;
  // getAllText(): Promise<string>;
}

// rip document sandbox api
// we were able to get the pages, but there
// is nothing in any of the fields???
